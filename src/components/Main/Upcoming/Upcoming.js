/* eslint-disable */
import React, {useState, Suspense} from 'react';
import {
  Text,
  View,
  TextInput,
  StyleSheet,
  TouchableOpacity,
  FlatList,
  //ScrollView,
} from 'react-native';

const Upcoming = () => {
  return (<View>
    <Text>Upcoming nè !</Text>
  </View>)
};

const styles = StyleSheet.create({

});

export default Upcoming;
