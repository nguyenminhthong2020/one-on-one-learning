/* eslint-disable */
import React, {useState, Suspense} from 'react';
import {
  Text,
  View,
  TextInput,
  StyleSheet,
  TouchableOpacity,
  FlatList,
  //ScrollView,
} from 'react-native';

const CourseDetail = () => {
 return(<View>
    <Text>Course Detail nè !</Text>
  </View>)
};

const styles = StyleSheet.create({

});

export default CourseDetail;
